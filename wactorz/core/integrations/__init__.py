"""Third-party service integrations (Home Assistant, Google, Gmail).

Explicit for the same reason as ``wactorz.interfaces``: implicit namespace
packages can absorb a same-named directory from elsewhere on ``sys.path``.
"""
